from setuptools import setup, find_packages

setup(
    name='alis',
    version='0.0.1',
    description='Aligning Latent and Image Spaces to Connect the Unconnectable',
    packages=find_packages(),
)
